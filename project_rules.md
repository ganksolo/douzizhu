# ... (保留你之前的代码规范等内容) ...

## 4. 交付协议 (Protocol: Definition of Done)

**Master Rule**: 任何代码或功能的编写，若未同步更新相关文档，视为**未完成 (Incomplete)**。严禁在未更新文档的情况下声称任务结束。

当用户指令中包含 **"DoD"**, **"Sync"**, **"交付"**, **"完成"** 等意图时，必须强制执行以下 **文档同步检查清单**：

### A. 全员通用 (Universal)
1.  **[进度] `@task.md`**: 必须勾选已完成的任务项，并检查是否需要拆解新的子任务。
2.  **[架构] `@implementation_plan.md`**: 如果代码结构、目录或技术选型发生了变化，必须修正此文件。
3.  **[产品] `@docs/product_features.md`**: 如果产出了新的业务价值（Feature），必须用**非技术语言**追加到此文档。

### B. 角色特有 (Role Specific)
* **前端 (Frontend)**:
    * 更新 **`@walkthrough.md`**: 如果 UI 交互流程有变，必须更新演示步骤。
    * 检查 **`@docs/api_spec.md`**: 如果前端发现缺字段，需在文档中标记（或通知后端）。
* **后端 (Backend)**:
    * **强制更新** **`@docs/api_spec.md`**: 任何接口变动必须第一时间同步。
    * 更新 **`@docs/backend_test_plan.md`**: 如果涉及核心逻辑变更，必须更新测试要点。
* **测试 (QA)**:
    * **强制更新** **`@docs/backend_test_plan.md`**: 必须记录新编写的测试用例、测试脚本路径以及测试结果日志（Pass/Fail）。

### C. 执行动作
每次任务结束时，请**自动**向用户汇报：“已执行 DoD 检查，更新了 [文件A, 文件B...]。”